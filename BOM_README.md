# BOM成本计算系统

基于FastAdmin框架开发的物料清单(BOM)成本计算系统，支持无限层级BOM结构、动态属性继承和精细化成本核算。

## 功能特色

### 核心功能
- **分层BOM管理**: 支持产品→部件→材料→工艺的无限层级结构
- **动态属性继承**: 通过公式实现尺寸属性的层级传递（L、W、H变量）
- **精细化成本核算**: 
  - 材料成本：支持按重量、面积、件数等多种计价方式
  - 工艺成本：支持固定成本、按件、按时、按面积等多种模型
  - 拼版数处理：材料成本可按拼版数分摊
- **灵活参数配置**: 支持动态设置损耗率、包装费、人工费等参数

### 技术特点
- **公式解析引擎**: 支持数学表达式和变量替换
- **双向计算流程**: 
  - 规格计算：自顶向下BOM树遍历解析尺寸
  - 成本计算：自底向上成本汇总
- **关联数据管理**: 完整的模型关联关系和数据完整性约束
- **用户友好界面**: 树形BOM管理、可视化成本展示

## 安装部署

### 1. 环境要求
- PHP 7.4+
- MySQL 5.7+
- FastAdmin框架

### 2. 数据库初始化
```sql
-- 1. 执行表结构创建
source bom_system_tables.sql;

-- 2. 插入演示数据（可选）
source demo_data.sql;
```

### 3. 文件部署
确保以下文件已正确部署到FastAdmin项目中：

```
application/
├── admin/
│   ├── controller/bom/          # BOM控制器
│   ├── model/                   # 数据模型
│   └── view/bom/               # 视图模板
└── common/library/costing/     # 核心计算逻辑

public/assets/js/backend/bom/   # 前端脚本
```

### 4. 权限配置
在FastAdmin后台中配置菜单权限：
- BOM管理 → 产品管理
- BOM管理 → 原材料管理  
- BOM管理 → 工艺管理
- BOM管理 → 部件管理
- BOM管理 → 材料使用
- BOM管理 → 工艺分配

## 使用指南

### 基础数据维护

#### 1. 原材料管理
- 维护材料基础信息：名称、类型、单位成本
- 设置物理属性：标准尺寸、克重、厚度、密度
- 选择计量单位：每公斤、每平方米、每件等

#### 2. 工艺管理
- 定义工艺类型：每单位可变、每面积可变、每时间可变、固定
- 设置成本参数：费率、设置费、吞吐率、损耗率

### BOM结构搭建

#### 3. 产品创建
- 创建产品并设置基础尺寸公式
- 尺寸公式示例：`297`（固定值）或 `=L*0.8`（基于变量）

#### 4. 部件层级构建
- 在产品下创建部件，支持多层嵌套
- 设置相对数量和尺寸继承公式
- 公式中可使用父级的L、W、H变量

#### 5. 材料分配
- 为部件分配所需材料
- 设置使用尺寸公式和拼版数
- 支持材料属性覆盖（克重、厚度等）

#### 6. 工艺分配
- 为部件或材料分配工艺流程
- 设置工艺顺序和成本覆盖
- 配置拼版影响和转换属性

### 成本计算

#### 7. 执行计算
- 在产品管理中点击"成本计算"
- 设置动态参数：包装费、人工费、损耗率等
- 查看详细的成本分解和汇总结果

## 公式语法

### 支持的变量
- `L`: 父级长度 (cm)
- `W`: 父级宽度 (cm)  
- `H`: 父级高度 (cm)

### 公式示例
```
固定值: 100
比例计算: =L*0.8
加减运算: =L+10
复合运算: =(L+W)*0.5
```

### 注意事项
- 公式以`=`开头表示动态计算
- 不以`=`开头的视为固定数值
- 变量名区分大小写
- 支持基础数学运算符：+、-、*、/、()

## 成本计算逻辑

### 计算流程
1. **规格解析**: 从产品开始，逐级解析所有节点的物理规格
2. **材料成本**: 根据使用量、单价和拼版数计算材料成本
3. **工艺成本**: 根据工艺类型和处理量计算工艺成本
4. **成本汇总**: 自底向上汇总各级成本，应用损耗和附加费用

### 成本类型
- **材料成本**: 基于材料规格和单价计算
- **工艺成本**: 根据工艺类型动态计算
- **附加成本**: 包装费、人工费、损耗摊销等

## 系统架构

### 核心类库
- `FormulaParser`: 公式解析和求值
- `SpecificationService`: 规格计算服务
- `CostingService`: 成本计算服务

### 数据模型
- `Product`: 产品模型
- `Component`: 部件模型
- `RawMaterial`: 原材料模型
- `MaterialUsage`: 材料使用模型
- `Process`: 工艺模型
- `ProcessAssignment`: 工艺分配模型

### 控制器架构
```
bom/
├── Product          # 产品管理（含成本计算）
├── Component        # 部件管理
├── RawMaterial      # 原材料管理
├── MaterialUsage    # 材料使用管理
├── Process          # 工艺管理
└── ProcessAssignment # 工艺分配管理
```

## 演示数据

系统提供了完整的演示数据，包括：
- 2个示例产品（A4包装盒、手机包装盒）
- 4种原材料（白卡纸、瓦楞纸、PET薄膜、EVA泡棉）
- 5个标准工艺（印刷、覆膜、模切、糊盒、质检）
- 完整的BOM结构和工艺流程

通过演示数据可以快速了解系统功能和操作流程。

## 扩展开发

### 添加新的计量单位
在 `RawMaterial` 模型的 `getUnitOfMeasureList()` 方法中添加新选项。

### 自定义工艺类型  
在 `Process` 模型的 `getCostTypeList()` 方法中添加新类型，并在 `CostingService` 中实现计算逻辑。

### 扩展成本参数
在成本计算界面和 `CostingService` 中添加新的参数处理逻辑。

## 技术支持

如需技术支持或功能定制，请联系开发团队。

## 版本信息
- 版本: v1.0.0  
- 框架: FastAdmin
- 开发语言: PHP 7.4+
- 数据库: MySQL 5.7+